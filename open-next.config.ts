import { defineCloudflareConfig } from "@opennextjs/cloudflare";

/**
 * Minimal OpenNext Cloudflare config.
 * Incremental cache / R2 can be added later without changing app routes.
 */
export default defineCloudflareConfig({});
